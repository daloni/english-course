name: ci

on: push

# Lo que necesita el despliegue a GitHub Pages del job `deploy`.
permissions:
  contents: read
  pages: write
  id-token: write

# Un despliegue a la vez, sin cancelar el que ya está publicando.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  ci:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [22]

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Install node
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Lint
        run: pnpm run lint

      - name: Typecheck
        run: pnpm run typecheck

      - name: Test
        run: pnpm run test

      # Solo main publica, así que solo main toca Pages: en el resto de ramas estos
      # dos pasos se saltan y la rama se queda en lint, typecheck, test y generate.
      - name: Configure Pages
        id: pages
        if: github.ref == 'refs/heads/main'
        uses: actions/configure-pages@v5
        with:
          # Da de alta el sitio de Pages si el repositorio aún no lo tiene, en vez
          # de fallar con un 404 esperando que alguien lo active a mano.
          enablement: true

      - name: Generate the static site
        run: pnpm run generate
        env:
          # El sitio cuelga de /<repo>/ en GitHub Pages, no de la raíz del dominio.
          # Fuera de main el paso anterior no corre, `base_path` viene vacío y el
          # sitio se genera contra la raíz, que es lo que queremos para comprobarlo.
          NUXT_APP_BASE_URL: ${{ steps.pages.outputs.base_path }}/

      - name: Upload the site
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v4
        with:
          path: .output/public

  # Solo la rama principal publica; el resto se queda en lint, test y generate.
  deploy:
    needs: ci
    if: github.ref == 'refs/heads/main'

    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
